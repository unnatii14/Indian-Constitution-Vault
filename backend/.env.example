# Google Gemini API Key for AI explanations
# Get your key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# App API Key for authentication (IMPORTANT!)
# Change this to a strong random key for production!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
APP_API_KEY=your_secure_api_key_here

# Note: The app will work without AI features if GEMINI_API_KEY is not set
# AI endpoints will return fallback messages
