// This file used to contain a remote API client. The app is now fully
// offline and uses `LocalDataService` instead. The file is intentionally
// kept (instead of deleted) so any older references still resolve, but it
// no longer contains any code.
